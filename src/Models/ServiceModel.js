export class ServiceModel{

    constructor(id, name, description, pageLink) {
      this.id = id;
      this.name = name;
      this.description = description;
      this.pageLink = pageLink;
    }
    
  }